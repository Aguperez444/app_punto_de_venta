[pytest]
minversion = 7.0
addopts = -ra -q --cov=app --cov-report=term-missing
python_files = test_*.py
pythonpath = .
markers =
    unit: tests unitarios rápidos con dobles
    integration: tests de integración con recursos reales
